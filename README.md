# Linear Ray Tracer

CPU-based ray tracer implemented in C++, featuring bounding volume hierarchy (BVH) acceleration and extensible material and geometry systems.

The renderer supports sphere and quadrilateral primitives, textured materials, and spatial acceleration using axis-aligned bounding boxes (AABB) for efficient ray intersection queries.

The project extends a basic ray tracing implementation with performance-focused data structures and a modular shading architecture.